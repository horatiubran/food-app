export const onSubmit = (event) => {
    event.preventDefault()
    // console.log("event", event)
    // console.log("value", event.target[0].value) // value of the first entry in the form (First Name)
}